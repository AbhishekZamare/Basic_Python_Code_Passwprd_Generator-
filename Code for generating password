import random
import string

pass_len = int(input("enter password lenght you want: "))
charVal= string.ascii_letters + string.digits + string.punctuation
password=""
for i in range(pass_len):
    password += random.choice(charVal)
    
print("Your optimum password for high sequrity is:", password)
